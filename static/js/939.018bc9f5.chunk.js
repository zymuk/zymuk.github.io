"use strict";(globalThis.webpackChunkzymuk_github_io||=[]).push([[939],{939(e,n,t){t.r(n),t.d(n,{default:()=>A});var o=t(43),a=t(408),i=t(583),r=t(892),s=t(53),l=t(688),c=t(521);const d={name:"OutputShader",uniforms:{tDiffuse:{value:null},toneMappingExposure:{value:1}},vertexShader:"\n\t\tprecision highp float;\n\n\t\tuniform mat4 modelViewMatrix;\n\t\tuniform mat4 projectionMatrix;\n\n\t\tattribute vec3 position;\n\t\tattribute vec2 uv;\n\n\t\tvarying vec2 vUv;\n\n\t\tvoid main() {\n\n\t\t\tvUv = uv;\n\t\t\tgl_Position = projectionMatrix * modelViewMatrix * vec4( position, 1.0 );\n\n\t\t}",fragmentShader:"\n\t\n\t\tprecision highp float;\n\n\t\tuniform sampler2D tDiffuse;\n\n\t\t#include <tonemapping_pars_fragment>\n\t\t#include <colorspace_pars_fragment>\n\n\t\tvarying vec2 vUv;\n\n\t\tvoid main() {\n\n\t\t\tgl_FragColor = texture2D( tDiffuse, vUv );\n\n\t\t\t// tone mapping\n\n\t\t\t#ifdef LINEAR_TONE_MAPPING\n\n\t\t\t\tgl_FragColor.rgb = LinearToneMapping( gl_FragColor.rgb );\n\n\t\t\t#elif defined( REINHARD_TONE_MAPPING )\n\n\t\t\t\tgl_FragColor.rgb = ReinhardToneMapping( gl_FragColor.rgb );\n\n\t\t\t#elif defined( CINEON_TONE_MAPPING )\n\n\t\t\t\tgl_FragColor.rgb = OptimizedCineonToneMapping( gl_FragColor.rgb );\n\n\t\t\t#elif defined( ACES_FILMIC_TONE_MAPPING )\n\n\t\t\t\tgl_FragColor.rgb = ACESFilmicToneMapping( gl_FragColor.rgb );\n\n\t\t\t#elif defined( AGX_TONE_MAPPING )\n\n\t\t\t\tgl_FragColor.rgb = AgXToneMapping( gl_FragColor.rgb );\n\n\t\t\t#endif\n\n\t\t\t// color space\n\n\t\t\t#ifdef SRGB_TRANSFER\n\n\t\t\t\tgl_FragColor = sRGBTransferOETF( gl_FragColor );\n\n\t\t\t#endif\n\n\t\t}"};class m extends c.o{constructor(){super();const e=d;this.uniforms=a.LlO.clone(e.uniforms),this.material=new a.D$Q({name:e.name,uniforms:this.uniforms,vertexShader:e.vertexShader,fragmentShader:e.fragmentShader}),this.fsQuad=new c.F(this.material),this._outputColorSpace=null,this._toneMapping=null}render(e,n,t){this.uniforms.tDiffuse.value=t.texture,this.uniforms.toneMappingExposure.value=e.toneMappingExposure,this._outputColorSpace===e.outputColorSpace&&this._toneMapping===e.toneMapping||(this._outputColorSpace=e.outputColorSpace,this._toneMapping=e.toneMapping,this.material.defines={},"srgb"===a.ppV.getTransfer(this._outputColorSpace)&&(this.material.defines.SRGB_TRANSFER=""),1===this._toneMapping?this.material.defines.LINEAR_TONE_MAPPING="":2===this._toneMapping?this.material.defines.REINHARD_TONE_MAPPING="":3===this._toneMapping?this.material.defines.CINEON_TONE_MAPPING="":4===this._toneMapping?this.material.defines.ACES_FILMIC_TONE_MAPPING="":6===this._toneMapping&&(this.material.defines.AGX_TONE_MAPPING=""),this.material.needsUpdate=!0),!0===this.renderToScreen?(e.setRenderTarget(null),this.fsQuad.render(e)):(e.setRenderTarget(n),this.clear&&e.clear(e.autoClearColor,e.autoClearDepth,e.autoClearStencil),this.fsQuad.render(e))}dispose(){this.material.dispose(),this.fsQuad.dispose()}}var v=t(463);var u=t(579);const p=[{name:"Aethera",kicker:"Oceanic dreamworld",description:"A luminous water planet whose equatorial currents fold into violet auroras and suspended atmospheric rivers.",accentA:"84, 226, 255",accentB:"160, 88, 255"},{name:"Pyra",kicker:"Living furnace world",description:"A carbon-black planet split by molten tectonic calligraphy, with incandescent matter breathing through every fracture.",accentA:"255, 132, 44",accentB:"255, 45, 86"},{name:"Orison",kicker:"Sacred ring architecture",description:"A pearl-and-teal giant encircled by a vast luminous archive: billions of particles arranged like celestial sheet music.",accentA:"255, 220, 142",accentB:"74, 232, 210"},{name:"Vesper",kicker:"Crystalline night engine",description:"A faceted violet world that stores starlight inside geometric continents and releases it through cyan polar seams.",accentA:"177, 116, 255",accentB:"76, 230, 255"}],f=2*Math.PI,h="\n        vec3 mod289(vec3 x) { return x - floor(x * (1.0 / 289.0)) * 289.0; }\n        vec4 mod289(vec4 x) { return x - floor(x * (1.0 / 289.0)) * 289.0; }\n        vec4 permute(vec4 x) { return mod289(((x * 34.0) + 1.0) * x); }\n        vec4 taylorInvSqrt(vec4 r) { return 1.79284291400159 - 0.85373472095314 * r; }\n\n        float saturate(float value) {\n            return clamp(value, 0.0, 1.0);\n        }\n\n        vec2 safeNormalize(vec2 value) {\n            return value * inversesqrt(max(dot(value, value), 1.0e-12));\n        }\n\n        vec3 safeNormalize(vec3 value) {\n            return value * inversesqrt(max(dot(value, value), 1.0e-12));\n        }\n\n        float safeAtan(float y, float x) {\n            bool nearOrigin = abs(x) + abs(y) < 1.0e-7;\n            return nearOrigin ? 0.0 : atan(y, x);\n        }\n\n        float sanitizeFloat(float value, float fallbackValue) {\n            bool valid = value == value && abs(value) < 1.0e5;\n            return valid ? value : fallbackValue;\n        }\n\n        vec3 sanitizeVec3(vec3 value, vec3 fallbackValue) {\n            bool invalid = any(notEqual(value, value))\n                || any(greaterThan(abs(value), vec3(1.0e5)));\n            return invalid ? fallbackValue : value;\n        }\n\n        vec3 limitLuminance(vec3 color, float maximumLuminance) {\n            vec3 safeColor = max(sanitizeVec3(color, vec3(0.0)), vec3(0.0));\n            float luminance = dot(safeColor, vec3(0.2126, 0.7152, 0.0722));\n            float scale = min(1.0, maximumLuminance / max(luminance, 1.0e-5));\n            return safeColor * scale;\n        }\n\n        float snoise(vec3 v) {\n            const vec2 C = vec2(1.0 / 6.0, 1.0 / 3.0);\n            const vec4 D = vec4(0.0, 0.5, 1.0, 2.0);\n\n            vec3 i = floor(v + dot(v, C.yyy));\n            vec3 x0 = v - i + dot(i, C.xxx);\n            vec3 g = step(x0.yzx, x0.xyz);\n            vec3 l = 1.0 - g;\n            vec3 i1 = min(g.xyz, l.zxy);\n            vec3 i2 = max(g.xyz, l.zxy);\n            vec3 x1 = x0 - i1 + C.xxx;\n            vec3 x2 = x0 - i2 + C.yyy;\n            vec3 x3 = x0 - D.yyy;\n\n            i = mod289(i);\n            vec4 p = permute(\n                permute(\n                    permute(i.z + vec4(0.0, i1.z, i2.z, 1.0))\n                    + i.y + vec4(0.0, i1.y, i2.y, 1.0)\n                )\n                + i.x + vec4(0.0, i1.x, i2.x, 1.0)\n            );\n\n            float n_ = 0.142857142857;\n            vec3 ns = n_ * D.wyz - D.xzx;\n            vec4 j = p - 49.0 * floor(p * ns.z * ns.z);\n            vec4 x_ = floor(j * ns.z);\n            vec4 y_ = floor(j - 7.0 * x_);\n            vec4 x = x_ * ns.x + ns.yyyy;\n            vec4 y = y_ * ns.x + ns.yyyy;\n            vec4 h = 1.0 - abs(x) - abs(y);\n            vec4 b0 = vec4(x.xy, y.xy);\n            vec4 b1 = vec4(x.zw, y.zw);\n            vec4 s0 = floor(b0) * 2.0 + 1.0;\n            vec4 s1 = floor(b1) * 2.0 + 1.0;\n            vec4 sh = -step(h, vec4(0.0));\n            vec4 a0 = b0.xzyw + s0.xzyw * sh.xxyy;\n            vec4 a1 = b1.xzyw + s1.xzyw * sh.zzww;\n            vec3 p0 = vec3(a0.xy, h.x);\n            vec3 p1 = vec3(a0.zw, h.y);\n            vec3 p2 = vec3(a1.xy, h.z);\n            vec3 p3 = vec3(a1.zw, h.w);\n            vec4 norm = taylorInvSqrt(vec4(\n                dot(p0, p0),\n                dot(p1, p1),\n                dot(p2, p2),\n                dot(p3, p3)\n            ));\n            p0 *= norm.x;\n            p1 *= norm.y;\n            p2 *= norm.z;\n            p3 *= norm.w;\n            vec4 m = max(0.6 - vec4(\n                dot(x0, x0),\n                dot(x1, x1),\n                dot(x2, x2),\n                dot(x3, x3)\n            ), 0.0);\n            m *= m;\n            return 42.0 * dot(\n                m * m,\n                vec4(\n                    dot(p0, x0),\n                    dot(p1, x1),\n                    dot(p2, x2),\n                    dot(p3, x3)\n                )\n            );\n        }\n\n        mat2 rotate2D(float angle) {\n            float s = sin(angle);\n            float c = cos(angle);\n            return mat2(c, -s, s, c);\n        }\n\n        mat3 rotateX3(float angle) {\n            float s = sin(angle);\n            float c = cos(angle);\n            return mat3(\n                1.0, 0.0, 0.0,\n                0.0, c, -s,\n                0.0, s, c\n            );\n        }\n\n        mat3 rotateZ3(float angle) {\n            float s = sin(angle);\n            float c = cos(angle);\n            return mat3(\n                c, -s, 0.0,\n                s, c, 0.0,\n                0.0, 0.0, 1.0\n            );\n        }\n    ",g="\n        const float PI = 3.141592653589793;\n        const float TWO_PI = 6.283185307179586;\n\n        float isPreset(float preset, float expected) {\n            return 1.0 - step(0.5, abs(preset - expected));\n        }\n\n        vec3 aetheraPosition(vec3 seed, vec4 randomData, float time) {\n            vec3 direction = safeNormalize(seed);\n            float latitude = asin(clamp(direction.y, -1.0, 1.0));\n            float continent = snoise(direction * 2.45 + vec3(0.0, time * 0.018, 0.0));\n            float currentNoise = snoise(\n                direction * 4.2\n                + vec3(time * 0.045, -time * 0.018, time * 0.032)\n            );\n            float current = sin(\n                latitude * 10.5\n                + dot(direction, safeNormalize(vec3(0.74, 0.11, 0.66))) * 5.4\n                + currentNoise * 1.35\n                - time * 0.18\n            );\n            float radius = 2.08 + continent * 0.072 + current * 0.026;\n            vec3 p = direction * radius;\n            p.y *= 0.975;\n            p.xz = rotate2D(sin(latitude * 3.0 + time * 0.08) * 0.018) * p.xz;\n            return p;\n        }\n\n        vec3 pyraPosition(vec3 seed, vec4 randomData, float time) {\n            vec3 direction = safeNormalize(seed);\n            float terrain = snoise(direction * 3.4 + vec3(time * 0.012, 0.0, 0.0));\n            float secondary = snoise(direction * 8.0 - vec3(0.0, time * 0.024, 0.0));\n            float moltenLift = pow(max(secondary * 0.5 + 0.5, 0.0), 8.0);\n            float radius = 2.08 + terrain * 0.125 + moltenLift * 0.090;\n            return direction * radius;\n        }\n\n        vec3 orisonBodyPosition(vec3 seed, vec4 randomData, float time) {\n            vec3 direction = safeNormalize(seed);\n            float latitude = asin(clamp(direction.y, -1.0, 1.0));\n            float bandNoise = snoise(\n                direction * vec3(2.2, 5.8, 2.2)\n                + vec3(time * 0.018, -time * 0.012, time * 0.014)\n            );\n            float band = sin(latitude * 18.0 + bandNoise * 1.8 + time * 0.06);\n            float storm = snoise(\n                direction * 4.15\n                + vec3(-time * 0.021, time * 0.013, time * 0.026)\n            );\n            float radius = 2.07 + band * 0.020 + storm * 0.038;\n            vec3 p = direction * radius;\n            p.y *= 0.945;\n            return p;\n        }\n\n        vec3 orisonRingPosition(vec3 seed, vec4 randomData, float time) {\n            float radius = mix(2.18, 3.62, pow(randomData.x, 0.74));\n            float angle = randomData.y * TWO_PI + time * (0.018 + randomData.w * 0.012);\n            float lane = floor(randomData.z * 11.0) / 11.0;\n            float gap = 0.92 + 0.08 * sin(lane * 71.0 + randomData.w * 16.0);\n            radius *= gap;\n            float vertical = (randomData.z - 0.5) * 0.115;\n            vertical += snoise(vec3(cos(angle) * 2.1, sin(angle) * 2.1, radius * 1.7)) * 0.035;\n            vec3 p = vec3(cos(angle) * radius, vertical, sin(angle) * radius);\n            p = rotateZ3(0.34) * rotateX3(0.20) * p;\n            return p;\n        }\n\n        vec3 vesperPosition(vec3 seed, vec4 randomData, float time) {\n            vec3 direction = safeNormalize(seed);\n            vec3 stepped = floor(direction * 7.0 + 0.5) / 7.0;\n            vec3 facetedDirection = safeNormalize(mix(direction, stepped, 0.68));\n            float cell = snoise(facetedDirection * 4.8);\n            vec3 axisA = safeNormalize(vec3(0.82, 0.31, 0.48));\n            vec3 axisB = safeNormalize(vec3(-0.24, 0.91, 0.34));\n            vec3 axisC = safeNormalize(vec3(0.41, -0.17, 0.90));\n            float lattice =\n                sin(dot(facetedDirection, axisA) * 18.0)\n                * sin(dot(facetedDirection, axisB) * 16.0)\n                * sin(dot(facetedDirection, axisC) * 14.0);\n            float seam = pow(abs(lattice), 5.5);\n            float radius = 2.06 + cell * 0.145 + seam * 0.070;\n            vec3 p = facetedDirection * radius;\n            p.y *= 1.018;\n            return p;\n        }\n\n        vec3 planetPosition(\n            float preset,\n            vec3 seed,\n            vec4 randomData,\n            float kind,\n            float time\n        ) {\n            if (preset < 0.5) return aetheraPosition(seed, randomData, time);\n            if (preset < 1.5) return pyraPosition(seed, randomData, time);\n            if (preset < 2.5) {\n                return kind > 0.5\n                    ? orisonRingPosition(seed, randomData, time)\n                    : orisonBodyPosition(seed, randomData, time);\n            }\n            return vesperPosition(seed, randomData, time);\n        }\n\n        vec3 aetheraColor(vec3 p, vec3 seed, vec4 randomData, float time) {\n            vec3 direction = safeNormalize(seed);\n            float latitude = asin(clamp(direction.y, -1.0, 1.0));\n            float current = snoise(\n                direction * 3.65\n                + vec3(-time * 0.055, time * 0.025, time * 0.04)\n            );\n            float ribbonNoise = snoise(\n                direction * 6.8\n                + vec3(time * 0.025, -time * 0.06, time * 0.015)\n            );\n            float auroraWave = sin(\n                latitude * 8.2\n                + dot(direction, safeNormalize(vec3(0.71, -0.08, 0.70))) * 7.1\n                + ribbonNoise * 1.8\n                - time * 0.35\n            );\n            float aurora = pow(max(0.0, auroraWave), 5.0);\n            float polar = smoothstep(0.48, 0.93, abs(direction.y));\n            vec3 abyss = vec3(0.004, 0.028, 0.13);\n            vec3 ocean = vec3(0.012, 0.34, 0.78);\n            vec3 cyan = vec3(0.05, 0.95, 1.25);\n            vec3 violet = vec3(0.58, 0.08, 1.05);\n            vec3 color = mix(abyss, ocean, current * 0.5 + 0.5);\n            color = mix(color, cyan, smoothstep(0.22, 0.80, current) * 0.55);\n            color += mix(cyan, violet, polar) * aurora * (0.28 + polar * 0.62);\n            color += vec3(0.08, 0.28, 0.52) * pow(max(0.0, snoise(direction * 9.0)), 5.0);\n            return limitLuminance(color, 1.38);\n        }\n\n        vec3 pyraColor(vec3 p, vec3 seed, vec4 randomData, float time) {\n            vec3 direction = safeNormalize(seed);\n            float crust = snoise(direction * 3.2 + vec3(time * 0.018, 0.0, 0.0));\n            float detail = abs(snoise(direction * 9.5 - vec3(0.0, time * 0.05, 0.0)));\n            float crack = 1.0 - smoothstep(0.035, 0.18, detail);\n            crack *= smoothstep(-0.32, 0.5, crust);\n            float ember = pow(max(0.0, snoise(direction * 17.0 + time * 0.08)), 7.0);\n            vec3 charcoal = mix(vec3(0.012, 0.006, 0.012), vec3(0.18, 0.028, 0.018), crust * 0.5 + 0.5);\n            vec3 molten = mix(vec3(1.45, 0.035, 0.002), vec3(1.55, 0.72, 0.055), crack);\n            vec3 color = charcoal;\n            color += molten * crack * 1.58;\n            color += vec3(1.35, 0.23, 0.025) * ember * 0.72;\n            return limitLuminance(color, 1.70);\n        }\n\n        vec3 orisonColor(vec3 p, vec3 seed, vec4 randomData, float kind, float time) {\n            if (kind > 0.5) {\n                float ringRadius = length(p.xz);\n                float lane = sin(ringRadius * 27.0 + randomData.w * 7.0);\n                float dust = snoise(vec3(\n                    cos(randomData.y * TWO_PI) * 2.2,\n                    sin(randomData.y * TWO_PI) * 2.2,\n                    ringRadius * 3.7\n                ));\n                vec3 antiqueGold = vec3(0.95, 0.52, 0.12);\n                vec3 mineralTeal = vec3(0.05, 0.76, 0.66);\n                vec3 duskViolet = vec3(0.33, 0.08, 0.55);\n                vec3 color = mix(antiqueGold, mineralTeal, lane * 0.5 + 0.5);\n                color = mix(color, duskViolet, smoothstep(0.50, 0.88, dust) * 0.42);\n                return limitLuminance(color, 1.12);\n            }\n\n            vec3 direction = safeNormalize(seed);\n            float latitude = asin(clamp(direction.y, -1.0, 1.0));\n            float latitudeMask = abs(latitude) / (PI * 0.5);\n            float broadFlow = snoise(\n                direction * 2.45\n                + vec3(time * 0.014, -time * 0.01, time * 0.018)\n            );\n            float fineFlow = snoise(\n                direction * 7.2\n                + vec3(-time * 0.032, time * 0.012, time * 0.024)\n            );\n            float band = sin(latitude * 18.0 + broadFlow * 2.4 + fineFlow * 0.55 + time * 0.045);\n            float storm = snoise(\n                direction * 4.3\n                + vec3(time * 0.025, time * 0.012, -time * 0.018)\n            );\n            float stormCell = smoothstep(0.34, 0.88, storm)\n                * (1.0 - smoothstep(0.72, 1.0, latitudeMask));\n\n            vec3 midnight = vec3(0.012, 0.028, 0.085);\n            vec3 bronze = vec3(0.58, 0.25, 0.055);\n            vec3 pearl = vec3(0.82, 0.76, 0.50);\n            vec3 deepTeal = vec3(0.012, 0.38, 0.46);\n            vec3 jade = vec3(0.035, 0.86, 0.66);\n\n            float brightBand = smoothstep(-0.30, 0.88, band);\n            vec3 color = mix(midnight, bronze, broadFlow * 0.5 + 0.5);\n            color = mix(color, pearl, brightBand * 0.48);\n            color = mix(color, deepTeal, smoothstep(0.06, 0.74, fineFlow) * 0.62);\n            color += jade * stormCell * 0.62;\n            color *= mix(1.0, 0.78, smoothstep(0.65, 1.0, latitudeMask));\n            return limitLuminance(color, 1.32);\n        }\n\n        vec3 vesperColor(vec3 p, vec3 seed, vec4 randomData, float time) {\n            vec3 direction = safeNormalize(seed);\n            float facet = floor((snoise(direction * 4.7) * 0.5 + 0.5) * 7.0) / 7.0;\n            float polar = smoothstep(0.48, 0.96, abs(direction.y));\n            float seamNoise = abs(snoise(direction * 10.0 + vec3(0.0, time * 0.025, 0.0)));\n            float seam = 1.0 - smoothstep(0.025, 0.14, seamNoise);\n            vec3 night = vec3(0.018, 0.006, 0.12);\n            vec3 violet = vec3(0.45, 0.045, 0.92);\n            vec3 amethyst = vec3(0.98, 0.18, 1.38);\n            vec3 cyan = vec3(0.03, 1.08, 1.48);\n            vec3 color = mix(night, violet, facet);\n            color = mix(color, amethyst, smoothstep(0.60, 0.96, facet) * 0.72);\n            color += cyan * seam * (0.38 + polar * 1.15);\n            color += cyan * pow(polar, 5.0) * 0.50;\n            return limitLuminance(color, 1.56);\n        }\n\n        vec3 planetColor(\n            float preset,\n            vec3 p,\n            vec3 seed,\n            vec4 randomData,\n            float kind,\n            float time\n        ) {\n            if (preset < 0.5) return aetheraColor(p, seed, randomData, time);\n            if (preset < 1.5) return pyraColor(p, seed, randomData, time);\n            if (preset < 2.5) return orisonColor(p, seed, randomData, kind, time);\n            return vesperColor(p, seed, randomData, time);\n        }\n    ",x=`\n    ${h}\n    ${g}\n\n    uniform float uTime;\n    uniform float uFromPreset;\n    uniform float uToPreset;\n    uniform float uTransition;\n    uniform float uTransitionEnergy;\n\n    varying vec3 vWorldPosition;\n    varying vec3 vObjectPosition;\n    varying vec3 vSeed;\n    varying vec3 vNormalApprox;\n    varying float vLocalMorph;\n    varying float vScanFront;\n    varying float vSweepCoordinate;\n\n    float phaseCoordinate(vec3 seed, float time) {\n        vec3 axisA = safeNormalize(vec3(0.82, 0.25, 0.52));\n        vec3 axisB = safeNormalize(vec3(-0.18, 0.93, 0.32));\n        float coordinate = dot(seed, axisA);\n        coordinate += sin(dot(seed, axisB) * 6.2 - time * 0.72) * 0.055;\n        coordinate += snoise(seed * 3.4 + vec3(0.0, time * 0.10, 0.0)) * 0.035;\n        return coordinate;\n    }\n\n    void main() {\n        vec3 seed = safeNormalize(position);\n        vec4 randomData = vec4(\n            fract(sin(dot(seed.xy, vec2(12.9898, 78.233))) * 43758.5453),\n            fract(sin(dot(seed.yz, vec2(39.3468, 11.135))) * 24634.6345),\n            fract(sin(dot(seed.zx, vec2(73.156, 52.235))) * 56445.234),\n            fract(sin(dot(seed.xyz, vec3(19.19, 7.17, 41.73))) * 9531.317)\n        );\n\n        vec3 fromPosition = sanitizeVec3(\n            planetPosition(uFromPreset, seed, randomData, 0.0, uTime),\n            seed * 2.08\n        );\n        vec3 toPosition = sanitizeVec3(\n            planetPosition(uToPreset, seed, randomData, 0.0, uTime),\n            seed * 2.08\n        );\n\n        float coordinate = phaseCoordinate(seed, uTime);\n        float scanPosition = mix(-1.24, 1.24, uTransition);\n        float scanWidth = 0.115;\n        float localMorph = 1.0 - smoothstep(\n            scanPosition - scanWidth,\n            scanPosition + scanWidth,\n            coordinate\n        );\n        float front = exp(-abs(coordinate - scanPosition) * 19.0);\n        float coreFront = exp(-abs(coordinate - scanPosition) * 42.0);\n\n        vec3 objectPosition = mix(fromPosition, toPosition, localMorph);\n        vec3 normalDirection = safeNormalize(objectPosition);\n        vec3 phaseAxis = safeNormalize(vec3(0.82, 0.25, 0.52));\n        vec3 tangent = safeNormalize(cross(normalDirection, phaseAxis + vec3(0.001, 0.002, 0.003)));\n        float ripple = sin(\n            dot(seed, safeNormalize(vec3(-0.31, 0.79, 0.53))) * 22.0\n            - uTime * 5.2\n        );\n        objectPosition += normalDirection * front * (0.052 + ripple * 0.022);\n        objectPosition += tangent * coreFront * ripple * 0.028;\n        objectPosition = sanitizeVec3(objectPosition, seed * 2.08);\n\n        vec4 worldPosition = modelMatrix * vec4(objectPosition, 1.0);\n        gl_Position = projectionMatrix * viewMatrix * worldPosition;\n\n        vWorldPosition = worldPosition.xyz;\n        vObjectPosition = objectPosition;\n        vSeed = seed;\n        vNormalApprox = safeNormalize(mat3(modelMatrix) * objectPosition);\n        vLocalMorph = localMorph;\n        vScanFront = front;\n        vSweepCoordinate = coordinate;\n    }\n`,y=`\n    ${h}\n    ${g}\n\n    uniform float uTime;\n    uniform float uFromPreset;\n    uniform float uToPreset;\n    uniform float uTransition;\n    uniform float uTransitionEnergy;\n\n    varying vec3 vWorldPosition;\n    varying vec3 vObjectPosition;\n    varying vec3 vSeed;\n    varying vec3 vNormalApprox;\n    varying float vLocalMorph;\n    varying float vScanFront;\n    varying float vSweepCoordinate;\n\n    void main() {\n        vec4 randomData = vec4(\n            fract(sin(dot(vSeed.xy, vec2(12.9898, 78.233))) * 43758.5453),\n            fract(sin(dot(vSeed.yz, vec2(39.3468, 11.135))) * 24634.6345),\n            fract(sin(dot(vSeed.zx, vec2(73.156, 52.235))) * 56445.234),\n            fract(sin(dot(vSeed.xyz, vec3(19.19, 7.17, 41.73))) * 9531.317)\n        );\n\n        vec3 fromColor = planetColor(\n            uFromPreset, vObjectPosition, vSeed, randomData, 0.0, uTime\n        );\n        vec3 toColor = planetColor(\n            uToPreset, vObjectPosition, vSeed, randomData, 0.0, uTime\n        );\n        vec3 baseColor = mix(fromColor, toColor, vLocalMorph);\n\n        vec3 normal = safeNormalize(vNormalApprox);\n        vec3 viewDirection = safeNormalize(cameraPosition - vWorldPosition);\n        vec3 keyDirection = safeNormalize(vec3(-0.70, 0.55, 0.85));\n        vec3 rimDirection = safeNormalize(vec3(0.65, -0.25, -0.70));\n\n        float ndl = dot(normal, keyDirection);\n        float diffuse = saturate(ndl);\n        float backLight = saturate(dot(normal, rimDirection));\n        float facing = saturate(dot(normal, viewDirection));\n        float fresnel = pow(max(1.0 - facing, 0.0), 3.0);\n        float terminator = smoothstep(-0.34, 0.56, ndl);\n\n        vec3 color = baseColor * (0.24 + diffuse * 1.04);\n        color *= mix(0.46, 1.0, terminator);\n        color += baseColor * backLight * 0.17;\n        color += mix(vec3(0.10, 0.48, 1.05), baseColor, 0.46) * fresnel * 0.66;\n\n        float micro = snoise(vSeed * 24.0 + vec3(0.0, uTime * 0.02, 0.0));\n        color += baseColor * micro * 0.060;\n\n        float thinCore = pow(clamp(vScanFront, 0.0, 1.0), 3.4);\n        float paletteMix = smoothstep(-0.78, 0.78, vSweepCoordinate);\n        vec3 phaseCyan = vec3(0.025, 0.92, 1.32);\n        vec3 phaseViolet = vec3(1.02, 0.055, 1.18);\n        vec3 phaseColor = mix(phaseCyan, phaseViolet, paletteMix);\n        color = mix(color, phaseColor, vScanFront * 0.38);\n        color += phaseColor * thinCore * 0.32;\n\n        float separation = exp(-abs(vScanFront - 0.72) * 22.0) * 0.10;\n        color *= 1.0 - separation;\n\n        color = limitLuminance(color, 1.76);\n        color = clamp(color, vec3(0.0), vec3(2.0));\n        gl_FragColor = vec4(color, 1.0);\n    }\n`,P=`\n    ${h}\n    ${g}\n\n    uniform float uTime;\n    uniform float uFromPreset;\n    uniform float uToPreset;\n    uniform float uTransition;\n    uniform float uTransitionEnergy;\n    uniform float uPixelRatio;\n    uniform float uPointScale;\n\n    attribute vec4 aRandom;\n    attribute float aKind;\n    attribute float aLayer;\n\n    varying vec3 vColor;\n    varying float vAlpha;\n    varying float vCore;\n    varying float vEnergy;\n    varying float vRibbon;\n\n    float phaseCoordinate(vec3 seed, float time) {\n        vec3 axisA = safeNormalize(vec3(0.82, 0.25, 0.52));\n        vec3 axisB = safeNormalize(vec3(-0.18, 0.93, 0.32));\n        float coordinate = dot(seed, axisA);\n        coordinate += sin(dot(seed, axisB) * 6.2 - time * 0.72) * 0.055;\n        coordinate += snoise(seed * 3.4 + vec3(0.0, time * 0.10, 0.0)) * 0.035;\n        return coordinate;\n    }\n\n    void main() {\n        vec3 seed = safeNormalize(position);\n        vec3 fromSurface = sanitizeVec3(\n            planetPosition(uFromPreset, seed, aRandom, aKind, uTime),\n            seed * 2.08\n        );\n        vec3 toSurface = sanitizeVec3(\n            planetPosition(uToPreset, seed, aRandom, aKind, uTime),\n            seed * 2.08\n        );\n\n        float fromRing = isPreset(uFromPreset, 2.0) * step(0.5, aKind);\n        float toRing = isPreset(uToPreset, 2.0) * step(0.5, aKind);\n        vec3 fromMatter = mix(fromSurface * mix(0.54, 1.0, aLayer), fromSurface, fromRing);\n        vec3 toMatter = mix(toSurface * mix(0.54, 1.0, aLayer), toSurface, toRing);\n\n        float delayedTransition = clamp(uTransition + (aRandom.w - 0.5) * 0.10, 0.0, 1.0);\n        float coordinate = phaseCoordinate(seed, uTime);\n        float scanPosition = mix(-1.24, 1.24, delayedTransition);\n        float localMorph = 1.0 - smoothstep(\n            scanPosition - 0.13,\n            scanPosition + 0.13,\n            coordinate\n        );\n        float front = exp(-abs(coordinate - scanPosition) * 15.5);\n        float coreFront = exp(-abs(coordinate - scanPosition) * 34.0);\n\n        vec3 objectPosition = mix(fromMatter, toMatter, localMorph);\n        vec3 normalDirection = safeNormalize(objectPosition);\n        vec3 phaseAxis = safeNormalize(vec3(0.82, 0.25, 0.52));\n        vec3 tangent = safeNormalize(cross(normalDirection, phaseAxis + vec3(0.001, 0.002, 0.003)));\n        float handedness = mix(-1.0, 1.0, step(0.5, aRandom.z));\n        float flutter = sin(\n            dot(seed, safeNormalize(vec3(-0.31, 0.79, 0.53))) * (16.0 + aRandom.x * 12.0)\n            - uTime * (4.0 + aRandom.y * 3.0)\n        );\n        objectPosition += normalDirection * front * (0.05 + aRandom.x * 0.24);\n        objectPosition += tangent * handedness * front * flutter * (0.035 + aRandom.y * 0.16);\n        objectPosition = sanitizeVec3(objectPosition, mix(fromMatter, toMatter, localMorph));\n\n        vec4 mvPosition = modelViewMatrix * vec4(objectPosition, 1.0);\n        gl_Position = projectionMatrix * mvPosition;\n\n        float distanceScale = 30.0 / max(1.0, -mvPosition.z);\n        gl_PointSize = (0.72 + aRandom.x * 1.65 + coreFront * 0.70)\n            * distanceScale * uPixelRatio * uPointScale;\n\n        vec3 fromColor = planetColor(\n            uFromPreset, fromSurface, seed, aRandom, aKind, uTime\n        );\n        vec3 toColor = planetColor(\n            uToPreset, toSurface, seed, aRandom, aKind, uTime\n        );\n        vec3 baseColor = mix(fromColor, toColor, localMorph);\n        vec3 phaseCyan = vec3(0.025, 0.90, 1.30);\n        vec3 phaseViolet = vec3(0.95, 0.055, 1.18);\n        vec3 phaseAmber = vec3(1.24, 0.38, 0.025);\n        vec3 phaseColor = aRandom.z < 0.60\n            ? mix(phaseCyan, phaseViolet, aRandom.z / 0.60)\n            : mix(phaseViolet, phaseAmber, (aRandom.z - 0.60) / 0.40);\n        vec3 color = mix(baseColor, phaseColor, 0.62 + coreFront * 0.18);\n        color = limitLuminance(color, 1.42);\n\n        vColor = clamp(sanitizeVec3(color, vec3(0.0)), vec3(0.0), vec3(1.60));\n        vAlpha = clamp(front * (0.026 + aRandom.w * 0.105), 0.0, 0.14);\n        vCore = 0.12 + coreFront * 0.24;\n        vEnergy = clamp(uTransitionEnergy, 0.0, 1.0);\n        vRibbon = coreFront;\n    }\n`,b=`\n    ${h}\n\n    uniform float uTime;\n    varying vec3 vDirection;\n\n    void main() {\n        vec3 direction = safeNormalize(vDirection);\n        float broad = snoise(direction * 1.7 + vec3(0.0, uTime * 0.006, 0.0));\n        float filaments = snoise(direction * 4.0 - vec3(uTime * 0.004, 0.0, 0.0));\n        float cloud = smoothstep(0.12, 0.88, broad * 0.7 + filaments * 0.3);\n        float horizon = pow(max(1.0 - abs(direction.y), 0.0), 4.0);\n        vec3 midnight = vec3(0.0015, 0.002, 0.009);\n        vec3 blue = vec3(0.018, 0.038, 0.10);\n        vec3 violet = vec3(0.055, 0.018, 0.10);\n        vec3 color = midnight;\n        color += mix(blue, violet, filaments * 0.5 + 0.5) * cloud * horizon * 0.62;\n        gl_FragColor = vec4(color, 1.0);\n    }\n`,w=`\n    ${h}\n    ${g}\n\n    uniform float uTime;\n    uniform float uFromPreset;\n    uniform float uToPreset;\n    uniform float uTransition;\n    uniform float uTransitionEnergy;\n    uniform float uPixelRatio;\n\n    attribute vec4 aRandom;\n    attribute float aLayer;\n    attribute float aKind;\n    varying vec3 vColor;\n    varying float vAlpha;\n    varying float vPulse;\n\n    float phaseCoordinate(vec3 seed, float time) {\n        vec3 axisA = safeNormalize(vec3(0.82, 0.25, 0.52));\n        vec3 axisB = safeNormalize(vec3(-0.18, 0.93, 0.32));\n        float coordinate = dot(seed, axisA);\n        coordinate += sin(dot(seed, axisB) * 6.2 - time * 0.72) * 0.055;\n        coordinate += snoise(seed * 3.4 + vec3(0.0, time * 0.10, 0.0)) * 0.035;\n        return coordinate;\n    }\n\n    void main() {\n        vec3 seed = safeNormalize(position);\n        vec3 fromSurface = planetPosition(uFromPreset, seed, aRandom, aKind, uTime);\n        vec3 toSurface = planetPosition(uToPreset, seed, aRandom, aKind, uTime);\n        float fromRing = isPreset(uFromPreset, 2.0) * step(0.5, aKind);\n        float toRing = isPreset(uToPreset, 2.0) * step(0.5, aKind);\n        vec3 fromMatter = mix(fromSurface * mix(0.72, 1.0, aLayer), fromSurface, fromRing);\n        vec3 toMatter = mix(toSurface * mix(0.72, 1.0, aLayer), toSurface, toRing);\n\n        float delayedTransition = clamp(uTransition + (aRandom.w - 0.5) * 0.075, 0.0, 1.0);\n        float coordinate = phaseCoordinate(seed, uTime);\n        float scanPosition = mix(-1.24, 1.24, delayedTransition);\n        float localMorph = 1.0 - smoothstep(scanPosition - 0.10, scanPosition + 0.10, coordinate);\n        float front = exp(-abs(coordinate - scanPosition) * 28.0);\n\n        vec3 p = mix(fromMatter, toMatter, localMorph);\n        vec3 normalDirection = safeNormalize(p);\n        vec3 phaseAxis = safeNormalize(vec3(0.82, 0.25, 0.52));\n        vec3 tangent = safeNormalize(cross(normalDirection, phaseAxis + vec3(0.002, 0.001, 0.003)));\n        float ray = step(0.73, aRandom.w);\n        float pulse = front * ray;\n        float wave = sin(aRandom.x * 22.0 + uTime * (5.0 + aRandom.y * 2.0));\n        p += normalDirection * pulse * (0.10 + aRandom.x * 0.42);\n        p += tangent * pulse * wave * (0.08 + aRandom.y * 0.24);\n\n        vec4 mvPosition = modelViewMatrix * vec4(sanitizeVec3(p, mix(fromMatter, toMatter, localMorph)), 1.0);\n        gl_Position = projectionMatrix * mvPosition;\n        gl_PointSize = (0.62 + aRandom.x * 1.45 + pulse * 0.80)\n            * uPixelRatio * (29.0 / max(1.0, -mvPosition.z));\n\n        vec3 cyan = vec3(0.035, 0.96, 1.40);\n        vec3 magenta = vec3(1.02, 0.035, 1.24);\n        vec3 amber = vec3(1.30, 0.40, 0.025);\n        vColor = aRandom.z < 0.56\n            ? mix(cyan, magenta, aRandom.z / 0.56)\n            : mix(magenta, amber, (aRandom.z - 0.56) / 0.44);\n        vAlpha = clamp(front * uTransitionEnergy * (0.012 + aRandom.w * 0.040), 0.0, 0.052);\n        vPulse = pulse;\n    }\n`,T=`\n    ${h}\n    ${g}\n\n    uniform float uTime;\n    uniform float uFromPreset;\n    uniform float uToPreset;\n    uniform float uTransition;\n    uniform float uTransitionEnergy;\n\n    varying vec3 vWorldPosition;\n    varying vec3 vNormalApprox;\n    varying float vLocalMorph;\n    varying float vFront;\n\n    float phaseCoordinate(vec3 seed, float time) {\n        vec3 axisA = safeNormalize(vec3(0.82, 0.25, 0.52));\n        vec3 axisB = safeNormalize(vec3(-0.18, 0.93, 0.32));\n        float coordinate = dot(seed, axisA);\n        coordinate += sin(dot(seed, axisB) * 6.2 - time * 0.72) * 0.055;\n        coordinate += snoise(seed * 3.4 + vec3(0.0, time * 0.10, 0.0)) * 0.035;\n        return coordinate;\n    }\n\n    void main() {\n        vec3 seed = safeNormalize(position);\n        vec4 randomData = vec4(0.17, 0.42, 0.73, 0.91);\n        vec3 fromPosition = sanitizeVec3(\n            planetPosition(uFromPreset, seed, randomData, 0.0, uTime),\n            seed * 2.08\n        );\n        vec3 toPosition = sanitizeVec3(\n            planetPosition(uToPreset, seed, randomData, 0.0, uTime),\n            seed * 2.08\n        );\n        float coordinate = phaseCoordinate(seed, uTime);\n        float scanPosition = mix(-1.24, 1.24, uTransition);\n        float localMorph = 1.0 - smoothstep(scanPosition - 0.14, scanPosition + 0.14, coordinate);\n        float front = exp(-abs(coordinate - scanPosition) * 17.0);\n        vec3 objectPosition = mix(fromPosition, toPosition, localMorph) * 1.065;\n        objectPosition = sanitizeVec3(objectPosition, seed * 2.20);\n\n        vec4 worldPosition = modelMatrix * vec4(objectPosition, 1.0);\n        gl_Position = projectionMatrix * viewMatrix * worldPosition;\n        vWorldPosition = worldPosition.xyz;\n        vNormalApprox = safeNormalize(mat3(modelMatrix) * objectPosition);\n        vLocalMorph = localMorph;\n        vFront = front;\n    }\n`,C=`\n    ${h}\n\n    uniform float uFromPreset;\n    uniform float uToPreset;\n    uniform float uTransitionEnergy;\n\n    varying vec3 vWorldPosition;\n    varying vec3 vNormalApprox;\n    varying float vLocalMorph;\n    varying float vFront;\n\n    vec3 atmosphereColor(float preset) {\n        if (preset < 0.5) return vec3(0.055, 0.42, 0.72);\n        if (preset < 1.5) return vec3(0.72, 0.10, 0.018);\n        if (preset < 2.5) return vec3(0.035, 0.40, 0.36);\n        return vec3(0.30, 0.08, 0.68);\n    }\n\n    void main() {\n        vec3 normal = safeNormalize(vNormalApprox);\n        vec3 viewDirection = safeNormalize(cameraPosition - vWorldPosition);\n        float facing = saturate(dot(normal, viewDirection));\n        float fresnel = pow(max(1.0 - facing, 0.0), 2.4);\n        vec3 color = mix(\n            atmosphereColor(uFromPreset),\n            atmosphereColor(uToPreset),\n            vLocalMorph\n        );\n        vec3 phaseTint = mix(vec3(0.03, 0.66, 1.02), vec3(0.72, 0.06, 0.94), vLocalMorph);\n        color = mix(color, phaseTint, vFront * 0.28);\n        float alpha = fresnel * (0.23 + vFront * 0.040);\n        color = limitLuminance(color, 1.02);\n        alpha = clamp(sanitizeFloat(alpha, 0.0), 0.0, 0.34);\n        gl_FragColor = vec4(color * fresnel * 0.92, alpha);\n    }\n`,M=`\n    ${h}\n\n    uniform float uTime;\n    uniform float uTransitionEnergy;\n    varying vec2 vLocal;\n    varying float vNoise;\n\n    void main() {\n        vec3 p = position;\n        float radius = length(p.xy);\n        float angle = safeAtan(p.y, p.x);\n        float warp = snoise(vec3(cos(angle), sin(angle), radius * 1.7 + uTime * 0.035));\n        p.z += warp * 0.032;\n        p.xy = rotate2D(uTime * 0.004) * p.xy;\n\n        vLocal = p.xy;\n        vNoise = warp;\n        gl_Position = projectionMatrix * modelViewMatrix * vec4(p, 1.0);\n    }\n`,z=`\n    ${h}\n\n    uniform float uTime;\n    uniform float uOpacity;\n    uniform float uTransitionEnergy;\n    varying vec2 vLocal;\n    varying float vNoise;\n\n    void main() {\n        float radius = length(vLocal);\n        vec2 ringDirection = safeNormalize(vLocal + vec2(0.00001));\n        float lane = sin(radius * 47.0 + sin(radius * 8.0) * 2.0);\n        float fineLane = sin(radius * 126.0 + (ringDirection.x * ringDirection.y) * 7.5);\n        float breakNoise = snoise(vec3(\n            ringDirection * 2.35,\n            radius * 4.8 + uTime * 0.03\n        ));\n        float gaps = smoothstep(-0.50, 0.16, breakNoise + lane * 0.28);\n        float edge = smoothstep(2.16, 2.31, radius) * (1.0 - smoothstep(3.48, 3.68, radius));\n\n        vec3 gold = vec3(0.92, 0.49, 0.10);\n        vec3 ice = vec3(0.035, 0.73, 0.65);\n        vec3 violet = vec3(0.34, 0.055, 0.55);\n        vec3 color = mix(gold, ice, lane * 0.5 + 0.5);\n        color = mix(color, violet, smoothstep(0.72, 0.98, fineLane) * 0.40);\n        color += vec3(0.34, 0.31, 0.19) * abs(fineLane) * 0.08;\n\n        float alpha = edge * gaps * (0.115 + abs(lane) * 0.145 + abs(fineLane) * 0.060);\n        alpha *= uOpacity;\n        color = limitLuminance(color, 1.05);\n        alpha = clamp(sanitizeFloat(alpha, 0.0), 0.0, 0.40);\n        gl_FragColor = vec4(color, alpha);\n    }\n`,A=()=>{(0,v.A)({title:"Celestial Transmutation",description:"A generative WebGL planet scene where four procedural worlds morph into each other through a phase-surge scan, orbital controls, bloom and auto-cycling",keywords:"three.js, webgl, shader, planets, generative, bloom, orbit, creative coding, animation"});const e=(0,o.useRef)(null);return(0,o.useEffect)(()=>{const n=e.current;if(!n)return;const t=window.matchMedia&&window.matchMedia("(prefers-reduced-motion: reduce)").matches||!1,o=n.clientWidth<760;let c,d,v,u,h,g,A,N,S,E,F,R,D,_,L,k,j,O,I,B,V,W,K,q,G,H,$=null;function U(){const e=o?1.45:1.85;return Math.min(window.devicePixelRatio||1,e)}try{v=new a.JeP({antialias:!0,alpha:!1,powerPreference:"high-performance"})}catch(pn){return}v.setPixelRatio(U()),v.setSize(n.clientWidth,n.clientHeight),v.setClearColor(131850,1),v.outputColorSpace="srgb",v.toneMapping=4,v.toneMappingExposure=1.08,n.appendChild(v.domElement),c=new a.Z58,c.fog=new a.cRK(131850,.015),d=new a.ubm(39,n.clientWidth/n.clientHeight,.08,90),d.position.set(0,.25,function(){const e=n.clientWidth/n.clientHeight;return e<.66?10.6:e<.95?9.55:8.55}()),u=new i.N(d,v.domElement),u.enableDamping=!0,u.dampingFactor=.045,u.enablePan=!1,u.enableZoom=!0,u.zoomSpeed=.72,u.minDistance=6.25,u.maxDistance=12.75,u.minPolarAngle=.16*Math.PI,u.maxPolarAngle=.84*Math.PI,u.autoRotate=!t,u.autoRotateSpeed=.24,u.target.set(0,.02,0),u.update();const Q=new a.Pq0,Y=new Set;let X=a.cj9.clamp(d.position.distanceTo(u.target),u.minDistance,u.maxDistance),Z=0,J=!1;function ee(e){Z=Math.max(Z,e)}function ne(){ee(14)}function te(e){"touch"===e.pointerType&&(Y.add(e.pointerId),Y.size>=2&&ee(18)),1===e.button&&(J=!0,ee(18))}function oe(e){"touch"===e.pointerType&&Y.size>=2&&ee(18),J&&ee(18)}function ae(e){"touch"===e.pointerType&&(Y.delete(e.pointerId),Y.size>0&&ee(8)),1===e.button&&(J=!1,ee(8))}function ie(e){"touch"===e.pointerType&&Y.delete(e.pointerId),0===e.buttons&&(J=!1)}function re(){ee(18)}function se(){ee(18)}v.domElement.addEventListener("wheel",ne,{passive:!0,capture:!0}),v.domElement.addEventListener("pointerdown",te,{passive:!0,capture:!0}),v.domElement.addEventListener("pointermove",oe,{passive:!0,capture:!0}),v.domElement.addEventListener("pointerup",ae,{passive:!0,capture:!0}),v.domElement.addEventListener("pointercancel",ae,{passive:!0,capture:!0}),v.domElement.addEventListener("pointerleave",ie,{passive:!0,capture:!0}),v.domElement.addEventListener("gesturestart",re,{passive:!0}),v.domElement.addEventListener("gesturechange",se,{passive:!0}),h=new r.s(v),h.addPass(new s.A(c,d)),g=new l.C(new a.I9Y(n.clientWidth,n.clientHeight),.5,.3,.5),h.addPass(g),h.addPass(new m);const le={uTime:{value:0},uFromPreset:{value:0},uToPreset:{value:0},uTransition:{value:0},uTransitionEnergy:{value:0}},ce=new a.BKk({uniforms:le,vertexShader:x,fragmentShader:y,transparent:!1,depthWrite:!0,depthTest:!0,side:0});E=ce;const de=new a.Gu$(45,48,32),me=new a.BKk({uniforms:{uTime:{value:0}},vertexShader:"\n    varying vec3 vDirection;\n\n    void main() {\n        vDirection = normalize(position);\n        gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);\n    }\n",fragmentShader:b,side:1,depthWrite:!1,fog:!1});N=me,j=new a.eaF(de,N),c.add(j);const ve=o?4800:8200,ue=new a.LoY,pe=new Float32Array(3*ve),fe=new Float32Array(ve),he=new Float32Array(ve);for(let e=0;e<ve;e+=1){const n=a.cj9.lerp(12,43,Math.pow(Math.random(),.62)),t=a.cj9.randFloatSpread(2),o=Math.random()*f,i=Math.sqrt(Math.max(0,1-t*t));pe[3*e]=Math.cos(o)*i*n,pe[3*e+1]=t*n,pe[3*e+2]=Math.sin(o)*i*n,fe[e]=.65+4.4*Math.pow(Math.random(),5),he[e]=Math.random()}ue.setAttribute("position",new a.THS(pe,3)),ue.setAttribute("aSize",new a.THS(fe,1)),ue.setAttribute("aSeed",new a.THS(he,1));const ge=new a.BKk({uniforms:{uTime:{value:0},uPixelRatio:{value:v.getPixelRatio()},uTransitionEnergy:{value:0}},vertexShader:"\n    uniform float uTime;\n    uniform float uPixelRatio;\n    uniform float uTransitionEnergy;\n    attribute float aSize;\n    attribute float aSeed;\n    varying float vAlpha;\n    varying vec3 vColor;\n\n    void main() {\n        vec3 p = position;\n        float drift = uTime * (0.003 + aSeed * 0.004);\n        float s = sin(drift);\n        float c = cos(drift);\n        p.xz = mat2(c, -s, s, c) * p.xz;\n\n        vec4 mvPosition = modelViewMatrix * vec4(p, 1.0);\n        gl_Position = projectionMatrix * mvPosition;\n        gl_PointSize = aSize * uPixelRatio * (18.0 / max(1.0, -mvPosition.z));\n        gl_PointSize *= 1.0 + uTransitionEnergy * (0.08 + aSeed * 0.10);\n\n        float twinkle = 0.55 + 0.45 * sin(uTime * (0.8 + aSeed * 2.0) + aSeed * 31.0);\n        vAlpha = (0.18 + aSeed * 0.58) * twinkle * (1.0 - uTransitionEnergy * 0.42);\n        vColor = mix(vec3(0.48, 0.65, 1.0), vec3(1.0, 0.82, 0.64), aSeed * aSeed);\n    }\n",fragmentShader:"\n    varying float vAlpha;\n    varying vec3 vColor;\n\n    void main() {\n        vec2 uv = gl_PointCoord - 0.5;\n        float distanceToCenter = length(uv);\n        if (distanceToCenter > 0.5) discard;\n        float core = exp(-distanceToCenter * 12.0);\n        float glow = exp(-distanceToCenter * 4.5) * 0.3;\n        gl_FragColor = vec4(vColor * (core + glow), vAlpha * (core + glow));\n    }\n",transparent:!0,blending:2,depthWrite:!1,fog:!1});S=ge,O=new a.ONl(ue,S),c.add(O),I=new a.YJl,I.rotation.z=-.055,c.add(I);const xe=new a.WBB(1,o?5:6);W=new a.eaF(xe,E),W.renderOrder=2,I.add(W);const ye=o?46e3:78e3,Pe=new a.LoY,be=new Float32Array(3*ye),we=new Float32Array(4*ye),Te=new Float32Array(ye),Ce=new Float32Array(ye);for(let e=0;e<ye;e+=1){const n=a.cj9.randFloatSpread(2),t=Math.random()*f,o=Math.sqrt(Math.max(0,1-n*n));be[3*e]=Math.cos(t)*o,be[3*e+1]=n,be[3*e+2]=Math.sin(t)*o,we[4*e]=Math.random(),we[4*e+1]=Math.random(),we[4*e+2]=Math.random(),we[4*e+3]=Math.random(),Ce[e]=.075+.925*Math.pow(Math.random(),1/3),Te[e]=Math.random()<.24?1:0}Pe.setAttribute("position",new a.THS(be,3)),Pe.setAttribute("aRandom",new a.THS(we,4)),Pe.setAttribute("aKind",new a.THS(Te,1)),Pe.setAttribute("aLayer",new a.THS(Ce,1)),Pe.boundingSphere=new a.iyt(new a.Pq0,8);const Me={...le,uPixelRatio:{value:v.getPixelRatio()},uPointScale:{value:o?.92:1}};F=new a.BKk({uniforms:Me,vertexShader:P,fragmentShader:"\n    varying vec3 vColor;\n    varying float vAlpha;\n    varying float vCore;\n    varying float vEnergy;\n    varying float vRibbon;\n\n    void main() {\n        vec2 uv = gl_PointCoord - 0.5;\n        float distanceToCenter = length(uv);\n        if (distanceToCenter > 0.5) discard;\n\n        float softGlow = exp(-distanceToCenter * mix(9.4, 7.2, vEnergy));\n        float core = (1.0 - smoothstep(0.0, 0.25, distanceToCenter)) * vCore;\n        float alpha = vAlpha * (softGlow * 0.76 + core * 0.30);\n        vec3 color = vColor * (softGlow * (0.88 + vRibbon * 0.12) + core * 0.42);\n        gl_FragColor = vec4(color, clamp(alpha, 0.0, 0.19));\n    }\n",transparent:!0,blending:1,depthWrite:!1,depthTest:!0}),B=new a.ONl(Pe,F),B.renderOrder=4,B.visible=!1,I.add(B);const ze=o?4200:7600,Ae=new a.LoY,Ne=new Float32Array(3*ze),Se=new Float32Array(4*ze),Ee=new Float32Array(ze),Fe=new Float32Array(ze);for(let e=0;e<ze;e+=1){const n=a.cj9.randFloatSpread(2),t=Math.random()*f,o=Math.sqrt(Math.max(0,1-n*n));Ne[3*e]=Math.cos(t)*o,Ne[3*e+1]=n,Ne[3*e+2]=Math.sin(t)*o,Se[4*e]=Math.random(),Se[4*e+1]=Math.random(),Se[4*e+2]=Math.random(),Se[4*e+3]=Math.random(),Ee[e]=.08+.92*Math.pow(Math.random(),1/3),Fe[e]=Math.random()<.22?1:0}Ae.setAttribute("position",new a.THS(Ne,3)),Ae.setAttribute("aRandom",new a.THS(Se,4)),Ae.setAttribute("aLayer",new a.THS(Ee,1)),Ae.setAttribute("aKind",new a.THS(Fe,1)),Ae.boundingSphere=new a.iyt(new a.Pq0,8);const Re={...le,uPixelRatio:{value:v.getPixelRatio()}};R=new a.BKk({uniforms:Re,vertexShader:w,fragmentShader:"\n    varying vec3 vColor;\n    varying float vAlpha;\n    varying float vPulse;\n    void main() {\n        vec2 uv = gl_PointCoord - 0.5;\n        float d = length(uv);\n        if (d > 0.5) discard;\n        float core = exp(-d * 15.0);\n        float glow = exp(-d * 8.2) * (0.12 + vPulse * 0.05);\n        gl_FragColor = vec4(vColor * (core + glow), clamp(vAlpha * (core + glow), 0.0, 0.075));\n    }\n",transparent:!0,blending:2,depthWrite:!1,depthTest:!0}),V=new a.ONl(Ae,R),V.renderOrder=5,V.visible=!1,I.add(V);const De={...le,uTime:{value:0},uFromAccent:{value:new a.Q1f(.1,.82,1)},uToAccent:{value:new a.Q1f(.72,.16,1)}};D=new a.BKk({uniforms:De,vertexShader:"\n    varying vec2 vUv;\n    void main() {\n        vUv = uv;\n        gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);\n    }\n",fragmentShader:"\n    uniform float uTime;\n    uniform float uTransition;\n    uniform float uTransitionEnergy;\n    uniform vec3 uFromAccent;\n    uniform vec3 uToAccent;\n    varying vec2 vUv;\n\n    float ringLine(float radius, float target, float width) {\n        return exp(-abs(radius - target) / max(width, 0.0001));\n    }\n\n    void main() {\n        vec2 p = (vUv - 0.5) * 2.0;\n        float radius = length(p);\n        float angle = atan(p.y, p.x);\n        float burst = exp(-pow((uTransition - 0.52) / 0.105, 2.0));\n        float progress = smoothstep(0.37, 0.72, uTransition);\n        float ringRadius = mix(0.18, 1.30, progress);\n        float warpedRadius = ringRadius\n            + sin(angle * 8.0 - uTime * 4.0) * 0.022\n            + sin(angle * 3.0 + uTime * 2.4) * 0.014;\n        float shock = ringLine(radius, warpedRadius, 0.017) * burst;\n        float echo = ringLine(radius, warpedRadius * 0.72, 0.013) * burst * 0.42;\n        float lens = exp(-radius * radius * 7.5) * burst;\n\n        vec3 color = mix(uFromAccent, uToAccent, smoothstep(0.28, 0.74, uTransition));\n        vec3 secondary = mix(vec3(0.03, 0.78, 1.0), vec3(0.82, 0.08, 0.96), smoothstep(0.30, 0.70, uTransition));\n        vec3 finalColor = color * shock * 0.58;\n        finalColor += secondary * echo * 0.34;\n        finalColor += mix(color, secondary, 0.5) * lens * 0.055;\n\n        float alpha = shock * 0.105 + echo * 0.055 + lens * 0.020;\n        alpha *= 1.0 - smoothstep(1.40, 1.62, radius);\n        if (alpha < 0.0015) discard;\n        gl_FragColor = vec4(finalColor, clamp(alpha, 0.0, 0.12));\n    }\n",transparent:!0,blending:2,depthWrite:!1,depthTest:!1,side:2}),G=new a.eaF(new a.bdM(5.6,5.6,1,1),D),G.renderOrder=8,G.visible=!1,c.add(G);const _e=new a.WBB(1,o?4:5);_=new a.BKk({uniforms:le,vertexShader:T,fragmentShader:C,transparent:!0,blending:2,side:1,depthWrite:!1}),K=new a.eaF(_e,_),K.renderOrder=3,I.add(K);const Le=new a.rKP(2.16,3.68,320,12),ke=new a.BKk({uniforms:{...le,uOpacity:{value:0}},vertexShader:M,fragmentShader:z,transparent:!0,blending:1,depthWrite:!1,side:2});L=ke,q=new a.eaF(Le,L),q.rotation.set(.5*Math.PI+.2,0,.34),q.renderOrder=1,I.add(q);const je=o?1200:2200,Oe=new a.LoY,Ie=new Float32Array(3*je),Be=new Float32Array(2*je);for(let e=0;e<je;e+=1){const n=Math.random()*f,t=a.cj9.lerp(3.9,5.8,Math.pow(Math.random(),.72));Ie[3*e]=Math.cos(n)*t,Ie[3*e+1]=a.cj9.randFloatSpread(.45),Ie[3*e+2]=Math.sin(n)*t,Be[2*e]=Math.random(),Be[2*e+1]=Math.random()}Oe.setAttribute("position",new a.THS(Ie,3)),Oe.setAttribute("aRandom",new a.THS(Be,2));const Ve=new a.BKk({uniforms:{uTime:{value:0},uPixelRatio:{value:v.getPixelRatio()},uTransitionEnergy:{value:0}},vertexShader:"\n    uniform float uTime;\n    uniform float uPixelRatio;\n    uniform float uTransitionEnergy;\n    attribute vec2 aRandom;\n    varying float vAlpha;\n    varying vec3 vColor;\n\n    void main() {\n        vec3 p = position;\n        float angle = uTime * (0.035 + aRandom.x * 0.055);\n        float s = sin(angle);\n        float c = cos(angle);\n        p.xz = mat2(c, -s, s, c) * p.xz;\n        p.y += sin(uTime * 0.45 + aRandom.y * 13.0 + length(p.xz)) * 0.13;\n        p *= 1.0 + uTransitionEnergy * 0.035;\n\n        vec4 mvPosition = modelViewMatrix * vec4(p, 1.0);\n        gl_Position = projectionMatrix * mvPosition;\n        gl_PointSize = (0.7 + aRandom.x * 1.5) * uPixelRatio * (25.0 / max(1.0, -mvPosition.z));\n        vAlpha = (0.05 + aRandom.y * 0.16) * (1.0 - uTransitionEnergy * 0.52);\n        vColor = mix(vec3(0.24, 0.48, 1.0), vec3(0.85, 0.35, 1.0), aRandom.x);\n    }\n",fragmentShader:"\n    varying float vAlpha;\n    varying vec3 vColor;\n\n    void main() {\n        vec2 uv = gl_PointCoord - 0.5;\n        float d = length(uv);\n        if (d > 0.5) discard;\n        float glow = exp(-d * 7.0);\n        gl_FragColor = vec4(vColor * glow, vAlpha * glow);\n    }\n",transparent:!0,blending:2,depthWrite:!1});k=Ve,H=new a.ONl(Oe,k),H.rotation.set(.18,0,-.28),I.add(H);const We={active:!1,from:0,to:0,queued:null,startTime:0,duration:t?.82:1.28,raw:0,eased:0,energy:0,current:0,settledAt:0};function Ke(e,n){le[e].value=n,Me[e].value=n,L.uniforms[e].value=n}function qe(e){const n=e.split(",").map(e=>Number(e.trim())/255);return new a.Q1f(Number.isFinite(n[0])?n[0]:.2,Number.isFinite(n[1])?n[1]:.7,Number.isFinite(n[2])?n[2]:1)}function Ge(e,n){De.uFromAccent.value.copy(qe(p[e].accentA)),De.uToAccent.value.copy(qe(p[n].accentB))}const He=document.createElement("input"),$e=document.createElement("span"),Ue=document.createElement("section"),Qe=document.createElement("div"),Ye=document.createElement("h1"),Xe=document.createElement("p"),Ze=document.createElement("div"),Je=document.createElement("div"),en=document.createElement("nav"),nn=[];const tn=function(){const e=document.createElement("div");e.className="celestial-ui";const t=document.createElement("label");t.className="glass-panel celestial-auto",t.title="Automatically visit the next planet";const o=document.createElement("span");return o.textContent="Auto",He.type="checkbox",He.checked=!0,He.className="celestial-auto-input",$e.className="celestial-auto-indicator",$e.setAttribute("aria-hidden","true"),t.appendChild(o),t.appendChild(He),t.appendChild($e),e.appendChild(t),Ue.className="celestial-copy",Ue.setAttribute("aria-live","polite"),Qe.className="celestial-kicker",Ye.className="celestial-name",Xe.className="celestial-description",Ze.className="celestial-meter",Ze.setAttribute("aria-hidden","true"),Je.className="celestial-meter-fill",Ze.appendChild(Je),Ue.appendChild(Qe),Ue.appendChild(Ye),Ue.appendChild(Xe),Ue.appendChild(Ze),e.appendChild(Ue),en.className="glass-panel celestial-planet-ui",en.setAttribute("aria-label","Conceptual planets"),p.forEach((e,n)=>{const t=document.createElement("button");t.type="button",t.className=0===n?"celestial-planet-btn is-active":"celestial-planet-btn",t.dataset.planet=String(n),t.setAttribute("aria-pressed",String(0===n)),t.textContent=e.name,t.addEventListener("click",()=>{sn(Number(t.dataset.planet),cn)}),nn.push(t),en.appendChild(t)}),e.appendChild(en),n.appendChild(e),e}();function on(e){const t=p[e];n.style.setProperty("--accent-a",t.accentA),n.style.setProperty("--accent-b",t.accentB)}function an(e,n){const t=p[e];Qe.textContent=n?"Phasing into "+t.kicker.toLowerCase():t.kicker,Ye.textContent=t.name,Xe.textContent=t.description}function rn(e,n){nn.forEach(t=>{const o=Number(t.dataset.planet),a=o===e&&null===n,i=null!==n&&o===n;t.classList.toggle("is-active",a),t.classList.toggle("is-target",i),t.setAttribute("aria-pressed",String(a||i))})}function sn(e,n){const t=(e+p.length)%p.length;We.active?We.queued=t:t!==We.current&&(We.active=!0,We.from=We.current,We.to=t,We.startTime=n,We.raw=0,We.eased=0,We.energy=0,Ge(We.from,We.to),Ke("uFromPreset",We.from),Ke("uToPreset",We.to),Ke("uTransition",0),Ke("uTransitionEnergy",0),an(t,!0),rn(We.current,t),on(t),Ze.classList.add("is-active"),Je.style.width="0%")}function ln(e){if(!We.active)return;const n=a.cj9.clamp((e-We.startTime)/We.duration,0,1),t=function(e){const n=a.cj9.clamp(e,0,1);return n<.5?.5*Math.pow(2*n,1.22):1-.5*Math.pow(2*(1-n),1.22)}(n),o=function(e){const n=a.cj9.clamp(e,0,1),t=Math.max(0,Math.sin(Math.PI*n)),o=Math.pow(t,1.35);return Number.isFinite(o)?o:0}(t);We.raw=n,We.eased=t,We.energy=o,Ke("uTransition",t),Ke("uTransitionEnergy",o),Je.style.width=100*n+"%",n>=1&&function(e){if(We.current=We.to,We.active=!1,We.raw=1,We.eased=1,We.energy=0,We.settledAt=e,Ke("uFromPreset",We.current),Ke("uToPreset",We.current),Ke("uTransition",0),Ke("uTransitionEnergy",0),an(We.current,!1),rn(We.current,null),on(We.current),Ze.classList.remove("is-active"),Je.style.width="100%",null!==We.queued){const n=We.queued;We.queued=null,n!==We.current&&sn(n,e+.001)}}(e)}let cn=0;const dn=e=>{const n=document.activeElement,t=n?n.tagName.toLowerCase():"";if("input"!==t&&"textarea"!==t)if("ArrowRight"===e.key||"ArrowDown"===e.key){e.preventDefault();sn((We.active?We.to:We.current)+1,cn)}else if("ArrowLeft"===e.key||"ArrowUp"===e.key){e.preventDefault();sn((We.active?We.to:We.current)-1,cn)}else if(" "===e.key){e.preventDefault();sn((We.active?We.to:We.current)+1,cn)}};function mn(){const e=n.clientWidth,t=n.clientHeight,o=U();d.aspect=e/t,d.updateProjectionMatrix(),v.setPixelRatio(o),v.setSize(e,t),h.setPixelRatio(o),h.setSize(e,t),Me.uPixelRatio.value=o,Re.uPixelRatio.value=o,S.uniforms.uPixelRatio.value=o,k.uniforms.uPixelRatio.value=o}window.addEventListener("keydown",dn),window.addEventListener("resize",mn,{passive:!0}),A=new a.zD7,We.settledAt=0;const vn=t?6:4.2,un=()=>{$=window.requestAnimationFrame(un);const e=Math.min(A.getDelta(),.04),n=A.elapsedTime;cn=n,ln(n);const t=We.energy;E.depthWrite=!0,B.visible=We.active,V.visible=We.active,le.uTime.value=n,Me.uTime.value=n,N.uniforms.uTime.value=n,S.uniforms.uTime.value=n,S.uniforms.uTransitionEnergy.value=t,k.uniforms.uTime.value=n,k.uniforms.uTransitionEnergy.value=t,De.uTime.value=n,G.visible=We.active,G.visible&&(G.quaternion.copy(d.quaternion),G.scale.setScalar(1));const o=We.active?Number(2===We.from):Number(2===We.current),i=We.active?Number(2===We.to):o,r=We.active?function(e){const n=a.cj9.clamp(e,0,1);return n*n*n*(n*(6*n-15)+10)}(We.eased):0;L.uniforms.uOpacity.value=a.cj9.lerp(o,i,r),I.rotation.y+=(.035+.12*t)*e,I.rotation.x=.025*Math.sin(.12*n),I.scale.set(1,1,1),H.rotation.y=.018*n,H.rotation.z=.055*Math.sin(.09*n)-.28,O.rotation.y=.0016*n,j.rotation.y=45e-5*-n,g.strength=.5+.055*t,g.radius=.3+.025*t,g.threshold=.5+.025*t,v.toneMappingExposure=1.08,He.checked&&!We.active&&n-We.settledAt>vn&&sn(We.current+1,n),u.update(),function(){Q.copy(d.position).sub(u.target);let e=Q.length();if((!Number.isFinite(e)||e<1e-6)&&(Q.set(0,.23,1).normalize(),e=X),Z>0)return X=a.cj9.clamp(e,u.minDistance,u.maxDistance),void(Z-=1);Math.abs(e-X)>1e-5&&(Q.multiplyScalar(1/e),d.position.copy(u.target).addScaledVector(Q,X),d.lookAt(u.target))}(),h.render()};return Ge(0,0),on(0),an(0,!1),rn(0,null),mn(),un(),()=>{window.cancelAnimationFrame($),window.removeEventListener("resize",mn),window.removeEventListener("keydown",dn),v.domElement&&(v.domElement.removeEventListener("wheel",ne,{capture:!0}),v.domElement.removeEventListener("pointerdown",te,{capture:!0}),v.domElement.removeEventListener("pointermove",oe,{capture:!0}),v.domElement.removeEventListener("pointerup",ae,{capture:!0}),v.domElement.removeEventListener("pointercancel",ae,{capture:!0}),v.domElement.removeEventListener("pointerleave",ie,{capture:!0}),v.domElement.removeEventListener("gesturestart",re),v.domElement.removeEventListener("gesturechange",se)),v.domElement.parentNode===n&&n.removeChild(v.domElement),tn.parentNode===n&&n.removeChild(tn),c&&c.traverse(e=>{e.geometry&&e.geometry.dispose();const n=e.material;Array.isArray(n)?n.forEach(e=>e.dispose()):n&&n.dispose()}),h&&h.dispose&&h.dispose(),g&&g.dispose&&g.dispose(),v&&v.dispose(),u&&u.dispose()}},[]),(0,u.jsx)("div",{className:"celestial-page",ref:e,children:(0,u.jsxs)("div",{className:"celestial-brand",children:[(0,u.jsx)("span",{className:"celestial-brand-mark"}),(0,u.jsxs)("div",{className:"celestial-brand-copy",children:[(0,u.jsx)("span",{className:"celestial-brand-title",children:"Celestial Transmutation"}),(0,u.jsx)("span",{className:"celestial-brand-subtitle",children:"Realtime phase-surge synthesis"})]})]})})}}}]);
//# sourceMappingURL=939.018bc9f5.chunk.js.map